#!/bin/bash
pkill -SIGINT -f 'ros2 bag record'
ARCHIVE_DIR="/common/ros_launcher/rosbags"
ALL_ARCH_DIRS=($(ls "$ARCHIVE_DIR"))
SAVE_PATH="${ALL_ARCH_DIRS[0]}"
ln -s "$SAVE_PATH" "$ARCHIVE_DIR/current"
